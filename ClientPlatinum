
public class ClientPlatinum extends Client {

	public ClientPlatinum(int id, String name, float balance) {
		super(id, name, balance, 0.01f, 0.05f);

	}

	@Override
	public String toString() {
		return "ClientPlatinum [getId()=" + getId() + "]";
	}

}
